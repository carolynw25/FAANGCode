import React from 'react';

function Profile() {
    return (
        <div>
            <h1>Profile</h1>
            {/* display firstname, lastname, username, email, password, etc */}
        </div>
    );
}

export default Profile;